# AI Provider API Keys (at least one required)
# The app will try providers in order: Gemini -> Groq -> Hugging Face

# Gemini API Key (Primary - may have quota limits)
# Get your free API key from: https://makersuite.google.com/app/apikey
# Use GEMINI_API_KEY for server-side only (recommended)
GEMINI_API_KEY=your_api_key_here

# Or use NEXT_PUBLIC_GEMINI_API_KEY for client-side access (not recommended for production)
# NEXT_PUBLIC_GEMINI_API_KEY=your_api_key_here

# Groq API Key (Free tier, very fast fallback)
# Get your free API key from: https://console.groq.com/keys
# Free tier: Llama 3.1 models, very fast responses
GROQ_API_KEY=your_groq_api_key_here

# Hugging Face API Key (Free tier, rate-limited fallback)
# Get your free API key from: https://huggingface.co/settings/tokens
# Free tier: ~300 requests/hour
HUGGINGFACE_API_KEY=your_huggingface_api_key_here
